main:
  environment: "prod"
  stage: "test"

admin-bot:
  debug: false
  timeout: 60
  parse_mode: markdown

public-bot:
  debug: false
  timeout: 60
  parse_mode: markdown

http:
  port: ":8080"
  timeout: 60
  
cache:
  ttl: "4h"
  admin_ttl: "720h"