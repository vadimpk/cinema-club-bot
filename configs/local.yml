main:
  environment: "local"
  stage: "test"

cache:
  ttl: "720h"
  admin_ttl: "720h"
  
admin-bot:
  debug: true
  timeout: 60
  parse_mode: markdown

public-bot:
  debug: true
  timeout: 60
  parse_mode: markdown


http:
  port: ":8080"
  timeout: 60